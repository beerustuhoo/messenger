{
  "name": "mobile-messenger",
  "private": true,
  "description": "Render entrypoint when Root Directory is repo root (prefer Root Directory: backend)",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node src/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
